#! /usr/bin/env sh

# script to updae (sync) main submodules of the project with
# origin master & origin luxcium & origin pc-master-race

(
  cd "${ahmyzsh}/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

(
  cd "${ahmyzsh}/custom-tmux/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

(
  cd "${ahmyzsh}/custom-zsh/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

(
  cd "${ahmyzsh}/nerd-fonts/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

(
  cd "${ahmyzsh}/node-repl/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

(
  cd "${ahmyzsh}/ohmyzsh/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

(
  cd "${ahmyzsh}/powerlevel10k/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

(
  cd "${ahmyzsh}/powerline/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

(
  cd "${ahmyzsh}/python-repl/" && git checkout master && git pull --all -t
  git checkout luxcium && git pull --all -t
  git checkout pc-master-race && git pull --all -t
  git push --tags --progress
  git push --all --progress
  git fetch -t
  git fetch --all -v -u
)

exit 0
